.footer {
    background-color: white;
    bottom: 32.1875px; /* TODO calculate based on height of StatusBar component? */
    display: flex;
    justify-content: center;
    right: 0;
    height: var(--page-footer-height);
    max-width: calc(100% - var(--navigation-bar-width));
    padding-bottom: 15px;
    padding-top: 15px;
    position: fixed;
    width: 100vw;
}
